version: "3.8"

services:
  dischargeiq:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
    volumes:
      - ./python:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
